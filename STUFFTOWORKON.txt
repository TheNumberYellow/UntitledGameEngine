GRAPHICS
    -Improve light performance further
        -Change point light attenuation to make smaller sphere

EDITOR
    -Drag to select (using frustum intersections)
    -Callback for directory updates to automatically update loaded meshes/textures (ReadDirectoryChangesW )

HALF-EDGE
    -"Carve" tool
    -Edge select

UI
    -Clean up/debug positioning + float dragger/slider behaviour specifically
    -Numerical text entry (add to float dragger)


ENTITY/BEHAVIOUR/COMPONENT SYSTEM
    -Editor accessible behaviour parameters


COLLISIONS
    -Continuous (swept) sphere collisions


NETWORKING
    -Track transforms of scene objects to only send when updated
    -Add FTP using windows' TransmitFile

CHANGE HOW I DO SHADERS SO CHANGES ARE EASIER
    -First: Move shaders into separate source files rather than in code
